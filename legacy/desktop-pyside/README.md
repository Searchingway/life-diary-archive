# PySide6 桌面 UI 历史说明

根目录 `main.py` 和 `src/life_dairy/` 的 Widgets UI 已冻结。不能整体移动 `src/life_dairy/`：当前 `diary_v2.0` 仍导入其 Storage、Models、Export 和 AI 兼容能力。

当前桌面应用位于 `diary_v2.0/`；完整边界见 [../../docs/CURRENT.md](../../docs/CURRENT.md)。

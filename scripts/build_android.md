# Android Build Notes

The Android project lives in `android/LifeDiaryMobile`.

```powershell
cd android/LifeDiaryMobile
.\build_android_arm64.bat
```

Generated APK files are release artifacts. Upload them to GitHub Releases instead of committing them to the source repository.

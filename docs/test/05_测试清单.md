# 测试清单 — Desktop 3.3 AI 辅助计划版

## 日记保存竞态回归（2026-07-19）

- [x] 自动保存 A 进行中继续编辑 B：A 成功后不清除 B 的未保存状态。
- [x] 自动、手动、切换前、新建前和图片保存请求按入队顺序串行执行。
- [x] 保存失败后下一次保存仍可执行；切换前保存失败时停留在当前日记。
- [x] 切换到另一篇日记后，旧请求响应不覆盖当前编辑器。
- [x] 空白新草稿不落盘；已有日记清空正文后仍可保存。
- [x] 同一日记并发保存后，metadata 与正文属于同一次完整请求；Windows 短暂占用时会重试原子替换。
- [x] 旧版 `content.md` 可读取；新保存使用版本化正文文件并由 `body_file` 指向。
- [x] metadata 写入失败时保留原 metadata 与原正文。

实际自动化命令：`npm run test -- --run`、`npm run build`、`python -B -m unittest discover -s tests -v`。

## AI 功能测试

### AI 配置
- [ ] 未配置 API Key 时点击 AI 按钮，提示配置 API Key
- [ ] 配置错误 API Key 时，测试连接失败，错误提示中文且不崩溃
- [ ] 正确配置后测试连接成功
- [ ] API Key 输入框为密码模式（不可见）
- [ ] 禁用 AI 后点击 AI 按钮，提示 AI 未启用

### AI 补全计划
- [ ] 空表单点击「AI 补全计划」，AI 生成默认计划草稿
- [ ] 填写部分内容后点击「AI 补全计划」，预览中显示 AI 补充内容
- [ ] 当前表单已有内容时，预览弹窗显示「将覆盖当前字段」警告
- [ ] 点击应用后，表单填入 AI 生成的内容
- [ ] 点击取消后，表单内容不变

### AI 整理思考
- [ ] 填写标题和描述后点击「AI 整理思考」，显示预览
- [ ] 确认后想法列表追加 AI 整理内容
- [ ] 确认后初步结论填入 AI 结论
- [ ] 取消后想法列表和结论不变

### AI 评估资源
- [ ] 填写标题和资源项后点击「AI 评估资源」，显示预览
- [ ] 确认后总体判断和轮回测试填入表单
- [ ] 取消后表单不变

### AI 拆解为行动计划
- [ ] 选择已有计划，点击「AI 拆解为行动计划」
- [ ] 输入开始日期、截止日期、每日可用时间
- [ ] AI 返回任务列表后显示预览
- [ ] 确认后自动创建行动计划，导航到行动计划页签
- [ ] AI 返回非法 JSON 时提示用户，不崩溃

### 行动计划 CRUD
- [ ] 新建行动计划：能创建空白草稿
- [ ] 保存行动计划：填写信息后保存成功
- [ ] 重启软件后行动计划仍存在
- [ ] 删除行动计划：确认弹窗后删除
- [ ] 子任务添加和勾选：添加任务 → 勾选完成 → 进度条更新
- [ ] 搜索：按标题/描述/任务搜索
- [ ] 状态筛选：按状态筛选行动计划
- [ ] 类型筛选：按计划类型筛选

### 总览页
- [ ] 基础统计显示行动计划总数、进行中、今日待办
- [ ] 今日行动任务列表显示今日未完成任务
- [ ] 双击今日任务可跳转到行动计划
- [ ] 时间线显示行动计划记录

## 新增测试项

### 基础 CRUD
- [ ] 新建接单记录：选择类型「接单记录」，填写客户、中介、执行人、报价等信息，能成功创建草稿
- [ ] 保存接单记录：填写完整后点击保存，状态栏提示已保存
- [ ] 重新打开后数据仍存在：关闭程序后重新打开，已保存的信息卡片出现在列表中
- [ ] 新建网课资源：选择类型「网课资源」，填写课程名称、平台、链接等信息
- [ ] 保存课程链接和平台：保存后重新打开，课程链接和平台字段正常显示
- [ ] 新建通用信息：选择类型「通用信息」，填写分类和主要内容
- [ ] 删除信息卡片：点击删除，确认后从列表中移除

### 搜索
- [ ] 搜索标题：在搜索框输入标题关键字，列表过滤正确
- [ ] 搜索客户 / 中介 / 课程平台：在搜索框输入客户名、中介名或课程平台，能搜索到对应记录

### 筛选
- [ ] 按类型筛选：选择「接单记录」/「网课资源」/「通用信息」，列表只显示对应类型
- [ ] 按状态筛选：选择特定状态，列表只显示对应状态的记录
- [ ] 类型筛选联动状态筛选：选择类型后，状态筛选下拉只显示该类型可用的状态

### 导航与集成
- [ ] 顶部导航能正常切换：点击「信息备忘」页签，页面正常显示；切换到其他页签再切回，数据正常
- [ ] 总览页能看到最近的信息备忘：总览页时间线中包含信息备忘记录，模块统计中显示信息备忘数量

### 自动保存
- [ ] 自动保存不跳光标：在编辑器中输入文字，等待 3 秒后自动保存，光标位置不发生变化
- [ ] 未保存内容切换提示：有未保存修改时切换页签，自动保存成功后允许切换

### 兼容性
- [ ] 旧模块不受影响：日记、足迹、轻计划、轻思考、轻资源、自我观察、教训与反思、自我分析、作品感悟等原有模块功能正常
- [ ] 数据管理：备份/恢复包含 info_memos/ 目录

## 回归测试（已有模块不受影响）

- [ ] 日记 CRUD
- [ ] 足迹 CRUD
- [ ] 轻计划 CRUD
- [ ] 轻思考 CRUD
- [ ] 轻资源 CRUD
- [ ] 自我观察 CRUD
- [ ] 教训与反思 CRUD
- [ ] 自我分析 CRUD
- [ ] 作品感悟 CRUD
- [ ] 总览页统计和时间线
- [ ] 数据管理备份/恢复

## 笔记功能测试项

### 基础 CRUD
- [ ] 新建笔记：点击「新建笔记」，三个输入框清空，处于可编辑状态
- [ ] 保存笔记：填写标题、描述、正文后点击保存，列表中出现新条目
- [ ] 重新打开后数据仍存在：关闭程序后重新打开，已保存的笔记出现在列表中
- [ ] 删除笔记：点击删除，确认后从列表中移除
- [ ] 恢复已保存内容：修改后点击「恢复已保存内容」，内容恢复到上次保存状态

### 搜索
- [ ] 搜索标题：在搜索框输入标题关键字，列表过滤正确
- [ ] 搜索描述和正文：搜索框可匹配描述和正文内容

### 自动保存
- [ ] 自动保存：输入文字后等待 3 秒，内容自动落盘
- [ ] 空白笔记不会自动保存：新建后不填写内容，不会创建存储文件
- [ ] 未保存内容切换提示：有未保存修改时切换页签，自动保存后允许切换

### 导航与集成
- [ ] 笔记页签标题随未保存状态变化：有修改时标签显示「笔记 *」
- [ ] 总览页能看到最近的笔记：总览页时间线中包含笔记记录
- [ ] 数据管理备份/恢复包含 notes/ 目录

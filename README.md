# Life Diary

当前应用、入口、数据位置与同步边界的唯一事实来源是 [docs/CURRENT.md](docs/CURRENT.md)。

日常使用：

- 桌面端：`cd diary_v2.0 && python launcher.py`
- 移动端：`cd mobile && npm install && npm run android`

根目录 `main.py`、`src/life_dairy/` 与 `legacy/` 是兼容或历史内容；请勿据此判断当前产品形态。真实用户数据和 API 配置均不提交 Git。
